# 项目开发说明

## 1. 项目介绍
-  该项目是一个WPF自定义控件库。

## 2. 基本要求
- 修改、新增任何控件后，必须更新CHANGELOG.md，CHANGELOG 中必须注明更新（修改）、新增时间，新增（修改）功能等信息。
- 必须更新README.md 文件，必须简短介绍控件的使用方式与独有依赖属性（功能）。
- 开发、修改控件必须遵守WPF官方规范，不能为了方便或快速实现功能而不遵守WPF推荐的自定义控件开发方式。
- 必须遵守当前控件库代码风格。
- 确保无Bug，必须经过测试才能验收。
- 必须保证UI风格统一。

## 其他要求
- 对于不明确的功能，必须提问。
- 对于可扩展的功能或依赖属性，必须询问是否扩展。
- 如果有更好的实现方式或当前实现方式不符合设计规范，必须询问，并检索是否有更好的方式，然后询问是否按照更优方式实现。
