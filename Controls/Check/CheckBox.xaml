<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Junevy.Controls.Controls.Check">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Junevy.Controls.Resources;component/Font/IconFont.xaml" />
        <ResourceDictionary Source="/Junevy.Controls;component/Themes/AppColors.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="DefaultCheckBoxTemplate" TargetType="{x:Type local:CheckBox}">
        <Grid Background="Transparent">
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">

                <!--  外框  -->
                <Border
                    x:Name="CheckBorder"
                    Width="18"
                    Height="18"
                    Background="Transparent"
                    BorderBrush="#AAAAAA"
                    BorderThickness="1.5"
                    CornerRadius="4">

                    <!--  勾选图标  -->
                    <TextBlock
                        x:Name="CheckMark"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource IconFont}"
                        FontSize="12"
                        Foreground="White"
                        Opacity="0"
                        Text="&#xe65d;" />
                </Border>

                <!--  文字  -->
                <ContentPresenter
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    RecognizesAccessKey="True" />

            </StackPanel>
        </Grid>

        <ControlTemplate.Triggers>

            <!--  Hover  -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#4A90E2" />
                <Setter TargetName="CheckBorder" Property="Background" Value="#F0F6FF" />
            </Trigger>

            <!--  Checked  -->
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="CheckBorder" Property="Background" Value="#4A90E2" />
                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#4A90E2" />
                <Setter TargetName="CheckMark" Property="Opacity" Value="1" />
            </Trigger>

            <!--  Checked + Hover  -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsChecked" Value="True" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="CheckBorder" Property="Background" Value="#357ABD" />
                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#357ABD" />
            </MultiTrigger>

            <!--  Disabled  -->
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#DDDDDD" />
                <Setter TargetName="CheckBorder" Property="Background" Value="#F5F5F5" />
                <Setter TargetName="CheckMark" Property="Foreground" Value="#BBBBBB" />
                <Setter Property="Foreground" Value="#BBBBBB" />
            </Trigger>

        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--<Style x:Key="DefaultCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource TextFillColor}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template" Value="{StaticResource DefaultCheckBoxTemplate}" />
    </Style>-->

    <Style x:Key="DefaultCheckBoxStyle" TargetType="{x:Type local:CheckBox}">
        <Setter Property="Foreground" Value="#333333" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style BasedOn="{StaticResource DefaultCheckBoxStyle}" TargetType="{x:Type local:CheckBox}" />
    <Style BasedOn="{StaticResource DefaultCheckBoxStyle}" TargetType="CheckBox" />
</ResourceDictionary>