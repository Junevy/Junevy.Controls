<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="clr-namespace:Junevy.Controls.AttachedProperties"
    xmlns:cvt="clr-namespace:Junevy.Controls.Converters"
    xmlns:local="clr-namespace:Junevy.Controls.Controls.Button">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Junevy.Controls;component/Themes/AppColors.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Thickness x:Key="Margin">3,0,3,0</Thickness>
    <Thickness x:Key="Padding">5</Thickness>
    <CornerRadius x:Key="CornerRadius">3</CornerRadius>
    <cvt:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    <cvt:IncreaseFontsizeConverter x:Key="IncreaseFontSize" />

    <!--  card button  -->
    <ControlTemplate x:Key="CardButtonTemplate" TargetType="{x:Type local:CardButton}">
        <Border
            x:Name="ContentBorder"
            Width="{TemplateBinding Width}"
            Height="{TemplateBinding Height}"
            MinWidth="{TemplateBinding MinWidth}"
            MinHeight="{TemplateBinding MinHeight}"
            Padding="{TemplateBinding Padding}"
            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
            VerticalAlignment="{TemplateBinding VerticalAlignment}"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            CornerRadius="{TemplateBinding Border.CornerRadius}"
            Opacity="1">
            <Grid Margin="5,0,5,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="2*" />
                    </Grid.RowDefinitions>


                    <ContentControl
                        Grid.Row="0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        Content="{TemplateBinding Title}"
                        FontFamily="{TemplateBinding FontFamily}"
                        FontSize="{TemplateBinding FontSize}"
                        Foreground="{TemplateBinding Foreground}" />

                    <ContentControl
                        Grid.Row="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        Content="{TemplateBinding Content}"
                        FontFamily="{TemplateBinding FontFamily}"
                        FontSize="{TemplateBinding FontSize,
                                                   Converter={StaticResource IncreaseFontSize}}"
                        Foreground="{TemplateBinding MainColor}" />


                </Grid>

                <Grid Grid.Column="1">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <ContentControl
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{Binding RelativeSource={RelativeSource AncestorType=local:CardButton}, Path=(atc:Icon.Icon)}"
                            FontFamily="{Binding RelativeSource={RelativeSource AncestorType=local:CardButton}, Path=(atc:Icon.FontFamily)}"
                            FontSize="{TemplateBinding FontSize,
                                                       Converter={StaticResource IncreaseFontSize}}"
                            Foreground="{TemplateBinding MainColor}" />
                    </Grid>

                </Grid>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <!--<Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ContentBorder" Property="Background" Value="{StaticResource IndicatorColor}" />
            </Trigger>

            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ContentBorder" Property="Background" Value="{StaticResource PressedBackground}" />
            </Trigger>-->

            <!--<Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="ContentBorder" Property="Background" Value="{StaticResource SubBackground}" />
                <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{StaticResource DisabledColor}" />
                <Setter TargetName="ContentBorder" Property="Opacity" Value="0.5" />
            </Trigger>-->
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <Style x:Key="DefaultButtonStyle" TargetType="{x:Type local:CardButton}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource DefaultControlFocusVisualStyle}" />
        <Setter Property="Background" Value="{StaticResource SubBackground}" />
        <Setter Property="Foreground" Value="{StaticResource TextFillColor}" />
        <!--<Setter Property="Height" Value="80" />-->
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="{StaticResource Padding}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}" />
        <Setter Property="Border.CornerRadius" Value="{StaticResource CornerRadius}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Margin" Value="5" />
        <Setter Property="Template" Value="{StaticResource CardButtonTemplate}" />

        <!--<Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:Button}, Path=(atc:AttachFuc.DispalyMode)}" Value="Icon">
                <Setter Property="Template" Value="{StaticResource IconButtonTemplate}" />
            </DataTrigger>

            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:Button}, Path=(atc:AttachFuc.DispalyMode)}" Value="Normal">
                <Setter Property="Template" Value="{StaticResource DefaultButtonTemplate}" />
            </DataTrigger>
        </Style.Triggers>-->
    </Style>

    <Style BasedOn="{StaticResource DefaultButtonStyle}" TargetType="{x:Type local:CardButton}" />


</ResourceDictionary>