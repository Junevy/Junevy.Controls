<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="clr-namespace:Junevy.Controls.AttachedProperties"
    xmlns:cvt="clr-namespace:Junevy.Controls.Converters"
    xmlns:local="clr-namespace:Junevy.Controls.Controls.DataGrid">

    <!--
        null / 空白字符串 → Collapsed：
        用于空态提示（atc:DataGridAssist.EmptyText）非空校验。
    -->
    <cvt:NullToCollapsedConverter x:Key="NullToVisibility" />

    <!--  列标题样式：Sunken 表头、底部分隔线、悬停高亮与排序方向指示箭头  -->
    <Style x:Key="DefaultDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource Theme.Brush.Surface.Sunken}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.Text.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border.Default}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter
                                Grid.Column="0"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            <Path
                                x:Name="SortArrow"
                                Grid.Column="1"
                                Width="9"
                                Height="6"
                                Margin="6,0,0,0"
                                VerticalAlignment="Center"
                                Data="M 0 5 L 4.5 0.5 L 9 5"
                                RenderTransformOrigin="0.5,0.5"
                                Stretch="None"
                                Stroke="{TemplateBinding Foreground}"
                                StrokeThickness="1.5"
                                Visibility="Collapsed" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Theme.Brush.Surface.Hover}" />
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible" />
                            <Setter TargetName="SortArrow" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  行头样式（HeadersVisibility=Row / All 时使用）  -->
    <Style x:Key="DefaultDataGridRowHeaderStyle" TargetType="DataGridRowHeader">
        <Setter Property="Background" Value="{DynamicResource Theme.Brush.Surface.Sunken}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border.Default}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="MinWidth" Value="24" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridRowHeader">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  左上角全选按钮样式（HeadersVisibility=All 时使用）  -->
    <Style x:Key="DefaultDataGridSelectAllButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource Theme.Brush.Surface.Sunken}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border.Default}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        单元格样式：
        背景透明，行悬停/选中色直接透出；底部细分隔线；键盘焦点时底部分隔线切换为主题焦点色。
    -->
    <Style x:Key="DefaultDataGridCellStyle" TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.Text.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border.Subtle}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource Theme.Brush.Accent.PrimarySubtle}" />
                <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.Text.Primary}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border.Focus}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  行样式：悬停高亮与选中背景（单元格透明，颜色直接透出）  -->
    <Style x:Key="DefaultDataGridRowStyle" TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="MinHeight" Value="36" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Theme.Brush.Surface.Hover}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource Theme.Brush.Accent.PrimarySubtle}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--
        DataGrid 控件模板（官方 Aero2 结构，部件契约见 WPF 官方源码）：

        Border
        └─ ScrollViewer（Focusable=false，自带自定义 Template）
           ├─ ScrollViewer.Template 内 3x3 Grid：
           │   (0,0) 全选按钮、(0,1) PART_ColumnHeadersPresenter —— 列头不随行滚动
           │   (1,0-1) PART_ScrollContentPresenter —— 行滚动视口
           │   (1,2) PART_VerticalScrollBar、(2,1) PART_HorizontalScrollBar
           └─ Content = ItemsPresenter —— 由它实例化 DataGrid 默认 ItemsPanel
               （DataGridRowsPresenter，运行时被 DataGrid 自动命名为 PART_RowsPresenter，
                经 OnIsItemsHostChanged 挂为 InternalItemsHost，并向上找到
                ScrollContentPresenter/ScrollViewer 接通滚动管线）

        注意：
        1. 模板内不得直接声明 DataGridRowsPresenter —— 非由 ItemsPresenter 实例化的
           面板 IsItemsHost=false，DataGrid 不会向其生成行容器。
        2. 列头必须位于 ScrollViewer 的模板内（而非其 Content），才能在垂直滚动时保持固定。
        3. ScrollViewer 模板内 TemplatedParent 是 ScrollViewer，
           引用 DataGrid 自身属性一律用 RelativeSource AncestorType={x:Type DataGrid}。
    -->
    <ControlTemplate x:Key="DefaultDataGridTemplate" TargetType="DataGrid">
        <Border
            Padding="{TemplateBinding Padding}"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            ClipToBounds="True"
            CornerRadius="{DynamicResource Theme.ControlCornerRadius}"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">
            <ScrollViewer x:Name="DG_ScrollViewer" Focusable="False">
                <ScrollViewer.Template>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--  HeadersVisibility=All 时的左上角全选按钮  -->
                            <Button
                                Command="{x:Static DataGrid.SelectAllCommand}"
                                Focusable="False"
                                Style="{StaticResource DefaultDataGridSelectAllButtonStyle}"
                                Visibility="{Binding HeadersVisibility,
                                                     ConverterParameter={x:Static DataGridHeadersVisibility.All},
                                                     Converter={x:Static DataGrid.HeadersVisibilityConverter},
                                                     RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />

                            <!--  列标题容器（固定不随行滚动）  -->
                            <DataGridColumnHeadersPresenter
                                x:Name="PART_ColumnHeadersPresenter"
                                Grid.Column="1"
                                Visibility="{Binding HeadersVisibility,
                                                     ConverterParameter={x:Static DataGridHeadersVisibility.Column},
                                                     Converter={x:Static DataGrid.HeadersVisibilityConverter},
                                                     RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />

                            <!--  行滚动视口：DataGrid 默认 ItemsPanel（DataGridRowsPresenter）由此呈现  -->
                            <ScrollContentPresenter
                                x:Name="PART_ScrollContentPresenter"
                                Grid.Row="1"
                                Grid.ColumnSpan="2"
                                CanContentScroll="{TemplateBinding CanContentScroll}" />

                            <!--  纵向滚动条（ScrollViewer 部件契约命名）  -->
                            <ScrollBar
                                x:Name="PART_VerticalScrollBar"
                                Grid.Row="1"
                                Grid.Column="2"
                                Orientation="Vertical"
                                Maximum="{TemplateBinding ScrollableHeight}"
                                Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ViewportSize="{TemplateBinding ViewportHeight}"
                                Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />

                            <!--  底部收边线：先声明置于滚动条之下，无横向滚动条时提供 1px 闭合线  -->
                            <Rectangle
                                Grid.Row="2"
                                Grid.ColumnSpan="3"
                                Height="1"
                                VerticalAlignment="Bottom"
                                Fill="{DynamicResource Theme.Brush.Border.Subtle}" />

                            <!--  横向滚动条：首列宽度跟随冻结列视口偏移，保证与列头对齐  -->
                            <Grid Grid.Row="2" Grid.Column="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="{Binding NonFrozenColumnsViewportHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <ScrollBar
                                    x:Name="PART_HorizontalScrollBar"
                                    Grid.Column="1"
                                    Orientation="Horizontal"
                                    Maximum="{TemplateBinding ScrollableWidth}"
                                    Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                    ViewportSize="{TemplateBinding ViewportWidth}"
                                    Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                            </Grid>

                            <!--
                                空态提示：Items 为空且 EmptyText 非空时显示在内容区中央。
                                位于 ScrollViewer 模板内，须以 AncestorType 引用 DataGrid。
                            -->
                            <TextBlock
                                Grid.Row="1"
                                Grid.ColumnSpan="2"
                                Margin="16"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="13"
                                Foreground="{DynamicResource Theme.Brush.Text.Tertiary}"
                                IsHitTestVisible="False"
                                Text="{Binding Path=(atc:DataGridAssist.EmptyText), RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding Items.Count, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" Value="0" />
                                                    <Condition
                                                        Binding="{Binding Path=(atc:DataGridAssist.EmptyText),
                                                                          RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},
                                                                          Converter={StaticResource NullToVisibility}}"
                                                        Value="Visible" />
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible" />
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </ControlTemplate>
                </ScrollViewer.Template>

                <!--
                    官方宿主链核心：ItemsPresenter 作为 ScrollViewer 的内容。
                    它实例化 DataGrid 静态构造函数中注册的默认 ItemsPanel
                    （DataGridRowsPresenter，自动命名 PART_RowsPresenter），
                    并将其放入 PART_ScrollContentPresenter 中呈现。
                -->
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
            </ScrollViewer>
        </Border>
    </ControlTemplate>

    <!--  DataGrid 控件样式  -->
    <Style x:Key="DefaultDataGridStyle" TargetType="DataGrid">
        <Setter Property="Template" Value="{StaticResource DefaultDataGridTemplate}" />
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource DefaultDataGridColumnHeaderStyle}" />
        <Setter Property="RowHeaderStyle" Value="{StaticResource DefaultDataGridRowHeaderStyle}" />
        <Setter Property="CellStyle" Value="{StaticResource DefaultDataGridCellStyle}" />
        <Setter Property="RowStyle" Value="{StaticResource DefaultDataGridRowStyle}" />
        <Setter Property="Background" Value="{DynamicResource Theme.Brush.Surface.Base}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.Text.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="GridLinesVisibility" Value="None" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="EnableRowVirtualization" Value="True" />
        <Setter Property="EnableColumnVirtualization" Value="True" />
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="CanUserAddRows" Value="False" />
        <Setter Property="CanUserDeleteRows" Value="False" />
        <Setter Property="CanUserResizeRows" Value="False" />
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="SelectionUnit" Value="FullRow" />
        <!--  官方行为：分组且分组内虚拟化关闭时强制按像素滚动，否则分组面板无法正确布局  -->
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsGrouping" Value="true" />
                    <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                </MultiTrigger.Conditions>
                <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!--  隐式样式（官方类型）：App 合并字典后 <DataGrid> 自动生效  -->
    <Style BasedOn="{StaticResource DefaultDataGridStyle}" TargetType="DataGrid" />

    <!--  隐式样式（派生类型）：jv:DataGrid 照常使用主题样式  -->
    <Style BasedOn="{StaticResource DefaultDataGridStyle}" TargetType="{x:Type local:DataGrid}" />

</ResourceDictionary>
