<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="clr-namespace:Junevy.Controls.AttachedProperties"
    xmlns:bt="clr-namespace:Junevy.Controls.Controls.Button"
    xmlns:cvt="clr-namespace:Junevy.Controls.Converters"
    xmlns:local="clr-namespace:Junevy.Controls.Controls.Text">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Junevy.Controls;component/Themes/AppColors.xaml" />
        <ResourceDictionary Source="/Junevy.Controls;component/Generic/Style/DefaultFocusStyle.xaml" />
        <ResourceDictionary Source="/Junevy.Controls;component/Controls/Button/Button.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <cvt:BoolToVisibilityConverter x:Key="BoolToVisibility" />

    <ControlTemplate x:Key="DefaultTextBoxTemplate" TargetType="{x:Type local:TextBox}">
        <Grid>
            <!--  外层 Border  -->
            <Border
                x:Name="Border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="5"
                SnapsToDevicePixels="True"
                UseLayoutRounding="True">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <ContentControl
                        Grid.Column="0"
                        Margin="5,0,0,0"
                        VerticalAlignment="Center"
                        Content="{Binding RelativeSource={RelativeSource AncestorType=local:TextBox}, Path=(atc:Icon.Icon)}"
                        Focusable="False"
                        FontFamily="{Binding RelativeSource={RelativeSource AncestorType=local:TextBox}, Path=(atc:Icon.FontFamily)}"
                        TextElement.FontSize="{TemplateBinding FontSize}"
                        TextElement.Foreground="{TemplateBinding Foreground}" />

                    <ScrollViewer
                        x:Name="PART_ContentHost"
                        Grid.Column="1"
                        Margin="0"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        HorizontalScrollBarVisibility="Hidden"
                        VerticalScrollBarVisibility="Hidden" />

                    <!--  占位符 TextBlock  -->
                    <TextBlock
                        x:Name="PlaceholderText"
                        Grid.Column="1"
                        Margin="{TemplateBinding Padding}"
                        Padding="2,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        FontSize="{TemplateBinding FontSize}"
                        Foreground="{StaticResource SubTextFillColor}"
                        IsHitTestVisible="False"
                        Text="{TemplateBinding Tag}"
                        Visibility="Collapsed" />

                    <!--  <bt:Button  -->
                    <bt:Button
                        x:Name="PART_CloseButton"
                        Grid.Column="2"
                        Height="{TemplateBinding Height}"
                        Margin="0,0,0,0"
                        HorizontalAlignment="Right"
                        Content="&#xe639;"
                        FontFamily="{Binding RelativeSource={RelativeSource AncestorType=local:TextBox}, Path=(atc:Icon.FontFamily)}"
                        Foreground="{TemplateBinding Foreground}"
                        Template="{StaticResource NoBorderButtonTemplate}"
                        Visibility="{Binding RelativeSource={RelativeSource AncestorType=local:TextBox}, Path=(atc:AttachFuc.IsClosable), Converter={StaticResource BoolToVisibility}}" />
                </Grid>
            </Border>


        </Grid>

        <ControlTemplate.Triggers>
            <!--  当 Text 为空 且 未获得焦点 时显示占位符  -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="Text" Value="" />
                    <Condition Property="IsFocused" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
            </MultiTrigger>

            <!--  焦点时边框高亮（现代感）  -->
            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AppColor}" />
                <Setter TargetName="Border" Property="BorderThickness" Value="1" />
                <Setter TargetName="Border" Property="Background" Value="{StaticResource MainBackground}" />
            </Trigger>


            <!--  鼠标悬停时轻微变化  -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="#BDBDBD" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <ControlTemplate x:Key="NoBorderTextBoxTemplate" TargetType="{x:Type local:TextBox}">
        <Border
            x:Name="TextBoxHost"
            BorderBrush="{StaticResource BorderBrushColor}"
            BorderThickness="0,0,0,0">
            <ScrollViewer
                x:Name="PART_ContentHost"
                Grid.Column="1"
                Margin="0"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                HorizontalScrollBarVisibility="Hidden"
                VerticalScrollBarVisibility="Hidden" />
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="NoBorderTextBoxTemplate_WPF" TargetType="TextBox">
        <Border
            x:Name="TextBoxHost"
            BorderBrush="{StaticResource BorderBrushColor}"
            Padding="5"
            BorderThickness="0,0,0,0">
            <ScrollViewer
                x:Name="PART_ContentHost"
                Grid.Column="1"
                Margin="0,0,0,0"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                HorizontalScrollBarVisibility="Hidden"
                VerticalScrollBarVisibility="Hidden" />
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsReadOnly" Value="False">
                <Setter TargetName="TextBoxHost" Property="BorderThickness" Value="0,0,0,2" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <Style x:Key="DefaultTextBoxStyle" TargetType="{x:Type local:TextBox}">
        <Setter Property="Background" Value="{StaticResource SubBackground}" />
        <Setter Property="Foreground" Value="{StaticResource TextFillColor}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}" />
        <Setter Property="BorderThickness" Value="0.5" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource DefaultControlFocusVisualStyle}" />
        <Setter Property="atc:AttachFuc.IsClosable" Value="True" />
        <Setter Property="Template" Value="{StaticResource DefaultTextBoxTemplate}" />
    </Style>


    <Style BasedOn="{StaticResource DefaultTextBoxStyle}" TargetType="{x:Type local:TextBox}" />
    <Style BasedOn="{StaticResource DefaultTextBoxStyle}" TargetType="TextBox" />

</ResourceDictionary>